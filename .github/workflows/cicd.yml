name: Deploy Node Application

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        run: docker login -u ${{secrets.DOCKER_USERNAME}} -p ${{secrets.DOCKER_PASSWORD}} 


      - name: Build Docker Image
        run: docker build -t riyashukla0803/back-end .

      - name: Publish Image to Docker Hub
        run: docker push riyashukla0803/back-end:latest

  deploy:
    needs: build
    runs-on: self-hosted
    steps:
      - name: Pull Image from Docker Hub
        run: docker pull riyashukla0803/back-end:latest

      - name: Run Docker Container
        run: "docker run -d -p 5000:5000 --name back-end-container -e MONGO_URI='mongodb+srv://riya70077:Mute123@backenddb.3tsx9c9.mongodb.net/?retryWrites=true&w=majority&appName=BackendDB' riyashukla0803/back-end" 